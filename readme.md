# TODO List using DDD and clean code

### Developer guide

- Install required packages
  `pnpm install`
- To run tests
  `pnpm run test`
- To run on dev mode
  `pnpm run dev`
- To start
  `pnpm run start`
